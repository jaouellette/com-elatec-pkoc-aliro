<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required for the post-transaction heads-up notification (Android 13+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Required for the full-screen result activity to launch over the lock
         screen when an Aliro transaction completes. Android 14+ also requires
         user approval of the special-use intent the first time it fires; that
         is handled by the OS, not by us. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <uses-feature
        android:name="android.hardware.nfc.hce"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <permission
        android:name="ActivitySignature"
        android:protectionLevel="signature|privileged" />

    <application
        android:name=".PKOC_Application"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <!-- PKOC NFC HCE service -->
        <service
            android:name=".PKOC_HostApduService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>

        <!-- Aliro NFC HCE service -->
        <service
            android:name=".Aliro_HostApduService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/aliro_apduservice" />
        </service>

        <!-- LEAF Verified NFC HCE service -->
        <!-- AID: D2 76 00 00 85 01 01 (LEAF Open App AID — placeholder per leaf_verified.h) -->
        <service
            android:name=".LeafVerifiedHceService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/leaf_apduservice" />
        </service>

        <activity
            android:name=".ConsentActivity"
            android:exported="true">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="mobile.opencredential.sentryinteractive.com"
                    android:pathPrefix="/share/" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Aliro BLE Credential Service (L2CAP) -->
        <service
            android:name=".AliroBleCredentialService"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".CredentialSelectionActivity" />

        <activity android:name=".AliroSelfTestActivity"
            android:label="Aliro Self-Test" />
        <activity android:name=".PKOCSelfTestActivity"
            android:label="PKOC Self-Test" />
        <activity android:name=".LeafSelfTestActivity"
            android:label="LEAF Verified Self-Test" />

        <activity android:name=".DiagnosticLogActivity"
            android:label="Diagnostic Log"
            android:exported="false" />

        <!-- Full-screen Aliro transaction result.
             showWhenLocked / turnScreenOn let it appear over the keyguard when
             launched via the full-screen intent from AliroTransactionFeedback.
             excludeFromRecents keeps it out of the recent-tasks list — the
             result is ephemeral, not something the user wants to return to.
             launchMode=singleTask so a fresh tap replaces any existing result. -->
        <activity android:name=".TransactionResultActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />

        <!-- Enrollment confirmation prompt — launched via full-screen intent
             when a reader sends a provisioning payload over the enrollment
             AID. Same lock-screen-override behavior as TransactionResultActivity
             so the prompt is visible whether the phone is idle or in use. -->
        <activity android:name=".EnrollmentConfirmActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />

        <!-- Cert-based enrollment confirmation prompt (Flow #2) — launched
             via full-screen intent when a reader sends an 0xE2 cert-request
             payload over the enrollment AID. Same lock-screen-override
             behavior as EnrollmentConfirmActivity. -->
        <activity android:name=".CertEnrollConfirmActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:exported="true"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
            </intent-filter>
            <meta-data android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/tech_list" />
        </activity>
    </application>

</manifest>
